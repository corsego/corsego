// Corsego Application JavaScript
// Bundled with Bun

// jQuery is loaded via CDN script tag in the layout
// This ensures it's available globally before this bundle runs
// A require shim is also set up in the layout for CommonJS modules

// Import all other modules
import "./application-modules.js"
