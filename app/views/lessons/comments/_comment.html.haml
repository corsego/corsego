.list-group-item{id: dom_id(comment)}
  = comment.created_at.strftime('%d-%m-%Y %H:%m')
  (#{time_ago_in_words(comment.created_at)} ago)
  - if comment.user.present?
    by
    %i.fa.fa-user{"aria-hidden": "true"}
    = link_to comment.user, user_path(comment.user)
  - if policy(comment).destroy?
    = link_to [@course, @lesson, comment], data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'btn btn-sm btn-danger' do
      %i.fa.fa-trash{"aria-hidden": "true"}
      %span.visually-hidden Delete comment
  = comment.content
