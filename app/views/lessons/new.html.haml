- content_for :title do
  = @course.title
  |
  New Lesson

%i.fa.fa-graduation-cap{"aria-hidden": "true"}
= link_to course_path(@course) do
  = @course.title
|
%i.far.fa-check-square{"aria-hidden": "true"}
= link_to new_course_lesson_path(@course, @lesson) do
  New lesson

.card.shadow
  .card-header
    %h3
      %i.far.fa-check-square{"aria-hidden": "true"}
      New lesson
  .card-body
    = render 'form'
  .card-footer
    = link_to 'Back to course', course_path(@course)
