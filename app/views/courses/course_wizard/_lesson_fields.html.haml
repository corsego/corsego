.nested-fields.container.rounded
  = f.input :title
  = f.association :chapter, collection: @course.chapters, include_blank: false
  = f.input :content, as: :rich_text_area
  .text-right
    = link_to_remove_association "remove lesson", f, class: 'btn btn-sm btn-danger mb-1'
