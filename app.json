{
  "name": "Corsego",
  "description": "Udemy-like online learning platform built with Ruby on Rails",
  "stack": "heroku-24",
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    },
    {
      "url": "heroku/ruby"
    }
  ],
  "addons": [
    "heroku-postgresql"
  ],
  "env": {
    "RAILS_MASTER_KEY": {
      "description": "Rails master key for decrypting credentials",
      "required": true
    }
  },
  "scripts": {
    "postdeploy": "bundle exec rails db:migrate"
  }
}
