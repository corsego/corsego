{
  "name": "Corsego",
  "description": "Online learning platform where anyone can publish and take courses",
  "stack": "heroku-24",
  "buildpacks": [
    {
      "url": "https://github.com/gaffneyc/heroku-buildpack-jemalloc.git"
    },
    {
      "url": "https://github.com/nickhstr/heroku-buildpack-bun.git"
    },
    {
      "url": "heroku/ruby"
    }
  ],
  "addons": [
    "heroku-postgresql"
  ],
  "env": {
    "RAILS_MASTER_KEY": {
      "description": "Rails master key for decrypting credentials",
      "required": true
    }
  },
  "scripts": {
    "postdeploy": "bundle exec rails db:migrate"
  }
}
